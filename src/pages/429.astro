---
import Layout from '../layouts/Layout.astro';
import Icon from '../components/global/Icon.astro';
---

<Layout
  title="429 - Too Many Requests"
  description="Slow down there! The workshop is getting too much traffic."
  noindex
>
  <section class="container mx-auto px-4 py-16 md:py-24">
    <div class="max-w-2xl mx-auto text-center">
      <div class="mb-8">
        <h1 class="text-6xl md:text-8xl font-bold text-accent mb-4">429</h1>
        <div class="w-24 h-1 bg-accent mx-auto mb-8"></div>
      </div>

      <h2 class="text-2xl md:text-3xl font-semibold mb-4 font-bebas">
        Workshop at Capacity
      </h2>

      <p class="text-lg md:text-xl text-ink-muted mb-12 leading-relaxed">
        Whoa there! The laser is working overtime. Please wait a moment before
        requesting another engraving.
      </p>

      <div
        class="flex flex-col sm:flex-row gap-4 justify-center items-center mb-12"
      >
        <button
          onclick="window.location.reload()"
          class="inline-flex items-center gap-2 px-6 py-3 bg-accent text-surface rounded-lg font-medium hover:bg-accent/90 transition-colors"
          aria-label="Try Again"
        >
          <Icon name="refresh" class="w-5 h-5" />
          Try Again
        </button>

        <a
          href="/"
          class="inline-flex items-center gap-2 px-6 py-3 border border-accent text-accent rounded-lg font-medium hover:bg-accent hover:text-surface transition-colors"
          aria-label="Back to Workshop"
        >
          <Icon name="home" class="w-5 h-5" />
          Back to Workshop
        </a>
      </div>

      <div class="bg-surface/50 border border-ink/20 rounded-lg p-6 mb-12">
        <h3 class="text-lg font-semibold mb-3 font-bebas">Rate Limiting</h3>
        <p class="text-ink-muted mb-4">
          To ensure everyone gets fair access to our custom engraving services,
          we limit the number of requests per visitor.
        </p>
        <div
          class="flex items-center justify-center gap-6 text-sm text-ink-faint"
        >
          <div class="flex items-center gap-2">
            <Icon name="clock" class="w-4 h-4" />
            <span>Please wait a moment</span>
          </div>
          <div class="flex items-center gap-2">
            <Icon name="shield" class="w-4 h-4" />
            <span>Protection enabled</span>
          </div>
        </div>
      </div>

      <div class="mt-8 pt-8 border-t border-border">
        <p class="text-sm text-ink-faint">
          If you believe this is an error, please <a
            href="mailto:studio@ditherandetch.ca"
            class="text-accent hover:underline"
            aria-label="contact us via email">contact us</a
          >.
        </p>
      </div>
    </div>
  </section>
</Layout>
