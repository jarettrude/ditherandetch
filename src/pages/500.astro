---
import Layout from '../layouts/Layout.astro';
import Icon from '../components/global/Icon.astro';
---

<Layout
  title="500 - Server Error"
  description="Something went wrong in the workshop. We're working to fix it."
  noindex
>
  <section class="container mx-auto px-4 py-16 md:py-24">
    <div class="max-w-2xl mx-auto text-center">
      <div class="mb-8">
        <h1 class="text-6xl md:text-8xl font-bold text-accent mb-4">500</h1>
        <div class="w-24 h-1 bg-accent mx-auto mb-8"></div>
      </div>

      <h2 class="text-2xl md:text-3xl font-semibold mb-4 font-bebas">
        Laser Misfire Detected
      </h2>

      <p class="text-lg md:text-xl text-ink-muted mb-12 leading-relaxed">
        Something went wrong in the engraving process. Our technicians have been
        alerted and are on the case.
      </p>

      <div
        class="flex flex-col sm:flex-row gap-4 justify-center items-center mb-12"
      >
        <a
          href="/"
          class="inline-flex items-center gap-2 px-6 py-3 bg-accent text-surface rounded-lg font-medium hover:bg-accent/90 transition-colors"
          aria-label="Back to Safety"
        >
          <Icon name="home" class="w-5 h-5" />
          Back to Safety
        </a>

        <button
          onclick="window.history.back()"
          class="inline-flex items-center gap-2 px-6 py-3 border border-accent text-accent rounded-lg font-medium hover:bg-accent hover:text-surface transition-colors"
          aria-label="Previous Page"
        >
          <Icon name="arrow-left" class="w-5 h-5" />
          Previous Page
        </button>
      </div>

      <div class="bg-surface/50 border border-ink/20 rounded-lg p-6 mb-12">
        <h3 class="text-lg font-semibold mb-3 font-bebas">Technical Details</h3>
        <p class="text-ink-muted mb-4">
          An unexpected error occurred while processing your request. This issue
          has been logged and our team will investigate.
        </p>
        <div
          class="flex items-center justify-center gap-6 text-sm text-ink-faint"
        >
          <div class="flex items-center gap-2">
            <Icon name="alert" class="w-4 h-4" />
            <span>Error logged</span>
          </div>
          <div class="flex items-center gap-2">
            <Icon name="tool" class="w-4 h-4" />
            <span>Investigation started</span>
          </div>
        </div>
      </div>

      <div class="mt-8 pt-8 border-t border-border">
        <p class="text-sm text-ink-faint">
          If this problem continues, please <a
            href="mailto:studio@ditherandetch.ca"
            class="text-accent underline hover:no-underline"
            aria-label="report the issue via email">report the issue</a
          >.
        </p>
      </div>
    </div>
  </section>
</Layout>
